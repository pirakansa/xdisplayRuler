[package]
name = "cli-template"
version = "0.1.0"
edition = "2021"
description = "Minimal Rust hello world template"
license = "MIT OR Apache-2.0"
repository = ""
readme = "README.md"

[profile.release]
codegen-units = 1
lto = true
opt-level = "z"
strip = true

